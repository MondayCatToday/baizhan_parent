server:
  port: 8087
spring:
  profiles:
    active: redis,commonspojo
  application:
    name: baizhan-frontend-cart
eureka:
  client:
    service-url:
      defaultZone: http://192.168.137.128:8761/eureka/
  instance:
    prefer-ip-address: true
baizhan:
  cart:
    keyPrefix: 'baizhan:cart::'
